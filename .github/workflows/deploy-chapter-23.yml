name: Deploy Chapter 23 — Reinforcement Learning

on:
  push:
    branches: [main]
    paths: ['chapter-23-reinforcement-learning/**']
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-chapter-template.yml
    with:
      chapter-dir: chapter-23-reinforcement-learning
      chapter-path: chapter-23
    secrets: inherit
