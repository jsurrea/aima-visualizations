name: Deploy Chapter 04 — Search in Complex Environments

on:
  push:
    branches: [main]
    paths: ['chapter-04-search-complex/**']
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-chapter-template.yml
    with:
      chapter-dir: chapter-04-search-complex
      chapter-path: chapter-04
    secrets: inherit
