name: Deploy Chapter 29 — The Future of AI

on:
  push:
    branches: [main]
    paths: ['chapter-29-future-ai/**']
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-chapter-template.yml
    with:
      chapter-dir: chapter-29-future-ai
      chapter-path: chapter-29
    secrets: inherit
