name: Deploy Chapter 22 — Deep Learning

on:
  push:
    branches: [main]
    paths: ['chapter-22-deep-learning/**']
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy-chapter-template.yml
    with:
      chapter-dir: chapter-22-deep-learning
      chapter-path: chapter-22
    secrets: inherit
