name: Deploy Chapter 12 — Quantifying Uncertainty

on:
  push:
    branches: [main]
    paths: ['chapter-12-uncertainty/**']
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    uses: ./.github/workflows/deploy-chapter-template.yml
    with:
      chapter-dir: chapter-12-uncertainty
      chapter-path: chapter-12
    secrets: inherit
